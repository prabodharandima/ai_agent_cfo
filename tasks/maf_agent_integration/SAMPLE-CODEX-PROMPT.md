```text
Work from the repository root.

This task continues the Microsoft Agent Framework integration.

Before editing:

1. Read the repository instruction files.
2. Read:
   tasks/maf_agent_integration/README.md
3. Read all earlier completed `TASK-MAF-*-RESULT.md` files.
4. Run:
   git status
   git log --oneline -10
5. Inspect only the current code relevant to this task. Treat the repository as the source of truth.

Then read and execute only:

tasks/maf_agent_integration/<TASK-FILE-NAME>.md

Preserve previous completed changes.
Do not repeat earlier work.
Do not revert earlier tasks.
Do not perform unrelated refactoring.
Keep token usage low by inspecting only relevant files and running focused tests first.

When finished, create the result file required by the task and report only:

- previous changes detected;
- files changed;
- implementation summary;
- tests and results;
- documentation updated;
- warnings;
- blockers;
- completion status.

Stop after this single task.
```

