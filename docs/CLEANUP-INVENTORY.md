# Cleanup Inventory

## Scope and method

This inventory is the discovery output for `TASK-CLEANUP-001`. No deletion or runtime, test, project, package, configuration, Docker, script, or frontend change is part of this task.

The repository was inspected through tracked-file enumeration, text/reference searches, project and package declarations, dependency-injection registrations, options binding, JSON serialization boundaries, EF Core migrations and design-time discovery, MCP attributes and capability discovery, health-check registrations, CLI argument handling, Docker Compose interpolation and profiles, PowerShell scripts, xUnit discovery attributes, React imports, Vite public assets, CSS selectors, npm scripts, and ignored/generated output. A low-reference symbol scan was used only as a lead; it was not treated as proof of non-use.

Search boundaries included `src/`, both `tools/` MCP projects, `tests/`, `scripts/`, both Compose files, `.env.example`, application settings, root/current documentation, historical phase documents, frontend source/configuration, NuGet project files, and npm manifests. The scan explicitly considered use by reflection, DI, configuration binding, serialization, EF tooling, MCP tool discovery, health checks, CLI commands, Docker, tests, and frontend build-time references.

## Baseline status

| Check | Result |
|---|---|
| Initial worktree | Clean; `git status --porcelain=v1` returned no entries. |
| Restore | Passed; all four solution projects were up to date. |
| Build | Passed for API, Finance MCP, Knowledge MCP, and tests with no reported warnings or errors. |
| Backend tests with Docker access | **Not green before this inventory:** 176 total, 161 passed, 7 failed, 8 skipped. The failures are caused by `src/CfoAgent.Api/appsettings.json` selecting Ollama while offline/default-provider tests require Mock; several chat tests consequently call Ollama or receive Ollama metadata. |
| Frontend install | `npm ci` completed successfully. |
| Frontend tests | Passed: 2 files, 10 tests. |
| Frontend build | Passed. |
| Frontend lint (additional evidence) | Passed with no unused-import or lint diagnostics. |

The backend baseline failure predates this document and is recorded below as stale configuration. Fixing it would violate this task's documentation-only scope.

## Safe-to-remove candidates

These candidates have direct evidence of non-use. Removal still belongs to a later cleanup task and must run the listed validation.

| Classification | Path | Symbol or item | Evidence | References searched | Risk | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Safe to remove / empty-folder anchor | `src/.gitkeep`, `tests/.gitkeep`, `tools/.gitkeep`, `data/knowledge/.gitkeep` | Redundant `.gitkeep` files | Each parent directory contains tracked files, so Git no longer needs an anchor. No code/config references the files. | `git ls-files`; repository-wide `.gitkeep` and path searches | Low | Delete the four redundant anchors. | `git status`; full .NET build/test; frontend build. |
| Safe to remove / empty folder | `data/imports/.gitkeep` and `data/imports/` | Unused import placeholder | The directory contains only `.gitkeep`; no source, Compose, script, configuration, test, or current documentation reference was found. | Runtime/test/script/Docker/docs search for `data/imports`, `data\\imports`, and `imports` | Low, unless an undocumented manual workflow depends on it | Remove the anchor and empty directory after confirming no external workflow uses it. | `git status`; full gates; manual review of user guide/import expectations. |
| Safe to remove / empty folder | `src/cfo-agent-ui/src/assets/` | Empty untracked Vite scaffold directory | Directory is empty, untracked, and has no import or URL reference. | Frontend import graph, public URL search, filesystem empty-directory scan | None to tracked repository; low locally | Remove the empty local directory. | Frontend test/build. |
| Safe to remove / frontend artifact | `src/cfo-agent-ui/public/icons.svg` | Unused Vite social/documentation icon sprite | No HTML, TS, TSX, CSS, test, Nginx, or Playwright reference exists. Only the file's own `<symbol>` declarations match. | Frontend `href`, `src`, `<use>`, `icons.svg`, and symbol searches | Low | Delete the unused sprite. | `npm test`; `npm run build`; Playwright container test. |
| Safe to remove / stale request file | `src/CfoAgent.Api/CfoAgent.Api.http` | `/weatherforecast/` sample request | The application does not map `/weatherforecast`; the file is a leftover ASP.NET template request and is not referenced by code/scripts/docs. | Endpoint mappings, repository-wide `weatherforecast` and file-name search | Low | Delete the request file or replace it only if a maintained API smoke file is desired. | API build; focused API tests; manual `/health/live` and `/api/chat` smoke checks. |
| Obsolete or duplicate test / safe to remove | `tests/CfoAgent.Api.Tests/UnitTest1.cs` | `MockModelMetadataTests.MockClientReportsTheConfiguredDeterministicModel` | Its two metadata assertions are already covered by `MockChatClientTests.ReturnsAStableUnsupportedResponseAndMockMetadata`; provider registration also has dedicated coverage. The filename is a template residue. | Mock test classes, metadata assertions, `MockChatClient.Metadata`, and `GetService(ChatClientMetadata)` | Low | Delete this duplicate test file. | Focused Mock tests; full backend suite; verify total test count decreases by exactly one. |

## Probably removable, but proof is still required

| Classification | Path | Symbol or item | Evidence | References searched | Risk | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Probably removable but requires proof / obsolete script path | `scripts/start-phase-5-e2e-api.ps1`, `src/cfo-agent-ui/playwright.config.ts`, `package.json` script `test:e2e` | Pre-Phase-8 local E2E startup path | The script calls the removed API `--seed` command, probes Chroma on an unpublished host port, and sets removed `Mcp__Finance__ServerProjectPath`. It is still referenced by the non-container Playwright config, while the current deployment uses `playwright.container.config.ts`. | Scripts, Playwright configs, package scripts, task/docs references, CLI handlers, Compose ports/config keys | Medium: deleting it also removes the local non-container E2E entry point | Prefer deleting the obsolete local harness and retaining the shared E2E spec plus container config, unless local E2E remains a requirement; otherwise rewrite it for Phase 8. | `npm run test:e2e:container`; full Compose deployment; grep for old CLI/config keys; update package/docs references. |
| Probably removable but requires proof / obsolete convenience scripts | `scripts/run-api.ps1`, `scripts/run-ui.ps1`, `scripts/build.ps1`, `scripts/test.ps1` | Unreferenced command wrappers | No current docs/tasks reference these four scripts. `build.ps1` and `test.ps1` omit the required serialized MSBuild flag; `run-api.ps1` alone cannot provide the required hosted dependencies. | Repository-wide script-name search, current README/user guide, Compose workflow | Medium: users may invoke them outside tracked documentation | Survey actual developer usage. Remove if unused; otherwise update them to the current documented workflow and serialized commands. | Execute every retained script; full backend/frontend gates; documentation link check. |
| Unused package candidate | `tests/CfoAgent.Api.Tests/CfoAgent.Api.Tests.csproj` | `coverlet.collector` 6.0.4 | No script, CI file, task, or current command invokes `--collect:"XPlat Code Coverage"`; normal test discovery does not require it. | Package references, scripts, docs, `CollectCoverage`, `coverlet`, and coverage command search | Medium: external CI may collect coverage without a tracked script | Confirm external CI expectations, then remove the package if coverage is not consumed. | Restore; full Debug/Release tests; run any external coverage job. |
| Obsolete or duplicate test / partial method candidate | `tests/CfoAgent.Api.Tests/AI/MockChatClientGateTests.cs` | `CurrentPhaseIntentsHaveStableOfflineResponses` theory | Its four intent/determinism cases are a subset of `MockChatClientTests.ClassifiesSupportedRequestsDeterministically`. The cancellation test in the same file is unique and must remain or move. | All Mock tests and inline-data cases | Low to medium | Remove only the duplicate theory or merge the unique cancellation case into `MockChatClientTests`. | Focused Mock tests; verify all seven intents, repeatability, delay cancellation, streaming, metadata, and simulated failure remain covered. |
| Obsolete or duplicate test candidate | `tests/CfoAgent.Api.Tests/SpecialistAgentTests.cs` | Two specialist-agent happy-path tests and `FinanceFake` | Sales/forecast happy paths substantially overlap `PhaseTwoAgentGateTests` and `AgentMcpWiringTests`; however, exact assertion coverage has not been reduced to a one-to-one matrix. | Specialist, Phase Two gate, MCP wiring, Ollama guardrail, and orchestrator tests | Medium | Build an assertion matrix first; remove only tests whose result contract, deterministic forecast, agent-name, and verified-data assertions are fully duplicated. | Focused agent/MCP tests; full suite; expected test-count delta review. |

## Stale configuration and active documentation

| Classification | Path | Symbol or item | Evidence | References searched | Risk | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Stale configuration | `src/CfoAgent.Api/appsettings.json` | `AI:Provider=Ollama`, `AI:Model=llama3.2:3b` | Binding rules, `.env.example`, and `AiProviderRegistrationTests.DefaultConfiguration_RegistersOnlyTheMockChatClient` define Mock as the committed default. The current values caused 7 backend failures and nondeterministic live Ollama calls in the required offline suite. | `AGENT.md`, architecture docs, `.env.example`, provider registration, app factory tests, Compose overrides | High until corrected: offline tests and default local behavior are not deterministic | Restore committed defaults to `Mock` / `DeterministicMock`; keep Ollama opt-in through `.env`, environment variables, or the example settings file. | Exact required backend command with Docker access; expect 176 total with only opt-in tests skipped; API root metadata smoke test. |
| Stale configuration | `.gitignore` | `data/*.db`, `data/*.db-shm`, `data/*.db-wal` | No SQLite package, runtime code, database file, migration, or connection string remains. These rules can hide an accidental reintroduction of local SQLite artifacts. | Runtime/package/config search for SQLite, DB files, and `Data Source=` | Low, but removal may expose developers' old local files | Remove the three SQLite-specific ignore patterns after checking local worktrees; explicitly clean or quarantine any surfaced legacy DB files. | `git status --ignored`; full gates; verify no DB file becomes tracked. |
| Stale configuration/documentation | `TEST_CASES.md` | Entire manual test guide | It describes SQLite, API `--seed`, disabled MCP/local finance fallback, host Chroma port 8000, and a removed `Mock LLM` badge. These conflict with Phase 8 Docker/PostgreSQL/HTTP MCP behavior. | Current Compose, CLI handlers, health checks, UI source, architecture/user guide | High for users following it | Rewrite around `docker compose up --build -d`, one-shot init services, PostgreSQL, internal MCP/Chroma ports, and current UI/provider behavior. | Execute every documented command/test against a clean deployment without deleting persistent user volumes. |
| Stale documentation | `docs/FUTURE-IMPROVEMENTS.md` | Ollama and PostgreSQL listed as unimplemented future work | Ollama support and Finance MCP-owned PostgreSQL are implemented. | Current projects/packages/config, Compose, Phase 7/8 results | Medium: roadmap is misleading | Remove completed items or mark them complete; retain genuinely future managed-provider/database options. | Documentation review against current architecture and Compose. |
| Stale documentation | `FILE-MANIFEST.md` | Manifest ends at Phase 6 task files | It omits all Phase 7, Phase 8, cleanup, runtime, MCP, frontend, Docker, and test files; `git ls-files` is the accurate inventory. | Full `git ls-files`, task directories, current solution | Low operational risk, high maintenance burden | Prefer removing this manually maintained manifest or redefine it narrowly and regenerate it. | Link/reference search; compare replacement to `git ls-files`. |
| Historical/stale navigation | `EXECUTION-ORDER.md` | Root execution order ends at Phase 6 | Phase 7, Phase 8, and cleanup have separate execution-order files, but the root document appears active and does not point to them. | Task directories, phase order files, current docs | Low to medium | Add explicit historical scope and links to later order files, or replace with one current index. | Documentation link review. |
| Stale frontend documentation | `src/cfo-agent-ui/README.md` | Local startup wording and `test:e2e` command | It tells users to start only API and Chroma and advertises the obsolete local E2E path; current runtime also requires both HTTP MCP services and PostgreSQL. | Root README/user guide, Playwright configs, Compose, package scripts | Medium | Update it to make full Compose/container E2E canonical; retain local Vite instructions against a running Docker API. | Execute all documented npm commands and container E2E. |

## Must-retain items and false positives

| Classification | Path | Symbol or item | Evidence | References searched | Risk if removed | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Must retain / reflection | `tools/*McpServer/*Tools.cs` | `[McpServerToolType]` classes and `[McpServerTool]` methods | `WithToolsFromAssembly()` discovers these without direct constructors. HTTP clients and contract tests discover exactly five Finance and two Knowledge tools. | MCP attributes, host registration, SDK discovery, contract/container tests | Critical | Retain. Do not classify low lexical reference counts as dead code. | HTTP capability discovery and all MCP host/container tests. |
| Must retain / EF generated and reflection | `tools/CfoAgent.FinanceMcpServer/Data/Migrations/*`, `FinanceDbContextFactory.cs` | Migration, designer, snapshot, design-time factory | EF runtime discovers migrations and tooling discovers `IDesignTimeDbContextFactory`; direct symbol counts are intentionally low. PostgreSQL migration tests depend on them. | EF attributes, `MigrateAsync`, design package, migration tests, seed initializer | Critical | Retain generated migration source and factory. | Fresh PostgreSQL migration/seed test; `dotnet ef` design-time command if changed. |
| Must retain / DI | `src/CfoAgent.Api/Health/*`, `Observability/*`, `ChatEndpoints.cs`, MCP interfaces/clients | Types mainly reached through DI, middleware, extension methods, and health registrations | `Program.cs` registers all items; tests replace interfaces through DI; endpoint/health routes exercise them. | `AddCheck<T>`, `AddExceptionHandler<T>`, middleware, `MapChatEndpoints`, service replacement tests | Critical | Retain. | API startup, readiness/liveness, Problem Details, and chat API tests. |
| Must retain / explicit development fallback | `KnowledgeFileMcpClient.cs`, `KnowledgeFileMcpAccess.cs`, `KnowledgeFileMcpFallback.cs`, `McpFallbackResult.cs`, `KnowledgeFilePathResolver.cs` | Secure local Knowledge fallback path | Containers disable fallback, but Development may explicitly enable it. Tests verify environment restriction, traversal protection, timeout/failure fallback, and cancellation propagation. | Options, DI, environment checks, fallback/access/security tests | High | Retain until the product explicitly removes the approved Development fallback. | Focused fallback/security tests in Development and Production. |
| Must retain / transport boundary DTOs | `FinanceMcpResults.cs`, `Features/Sales/SalesAnalysisResults.cs`, private DTOs in `FinanceMcpClient.cs`, Knowledge result envelopes | Similar-looking server, transport, and API result types | They isolate independently hosted MCP projects and JSON transport from the API's deterministic result contract. Sharing them would add coupling/project references that Phase 8 intentionally avoided. | JSON serialization/deserialization, mappings, project references, contract freeze tests | High | Retain the duplication; do not create a shared abstraction solely to remove it. | MCP HTTP contract and API contract-freeze tests. |
| Must retain / configuration and test seams | `Configuration/*.cs`, `appsettings.Development.json`, `appsettings.Ollama.example.json` | Options properties including `SimulatedDelayMilliseconds`, `SimulateFailure`, local fallback keys | Options are bound and validated; Mock delay/failure are used by controlled-error and cancellation tests; Ollama example supports opt-in host execution. | Options binding, `Program.cs`, tests, Compose/env keys, scripts | High | Retain keys that have runtime, test, or documented opt-in use. | Options validation, provider registration, Mock resilience tests, Compose config. |
| Must retain / RAG | `Rag/**`, Chroma configuration and service | Chroma client, deterministic embeddings, ingestion/retrieval result types | Used through DI and CLI `--ingest-rag`; Chroma remains the semantic source and citations path. | DI, ingestion CLI, retrieval agents, Chroma tests, Compose `rag-init` | Critical | Retain. | RAG unit/integration tests and cited knowledge workflow. |
| Must retain / Docker | `docker-compose.yml`, `docker-compose.integration.yml`, all Dockerfiles | PostgreSQL, one-shot init, two MCP services, Chroma, RAG init, API, frontend, profiles/networks/volumes/ports | Every service participates in startup or isolated tests. `full-deployment` intentionally suppresses frontend during the backend integration overlay; `container-tests` is invoked by the Phase 8 script. Only UI/API ports are published. | Compose dependencies/profiles, scripts, docs, container tests, health checks | Critical | Retain all current services, health checks, internal network, named volumes, and profile behavior. No dead service or port was found. | `docker compose config`; full deployment; isolated container gate; port-boundary checks. |
| Must retain / test discovery | xUnit fixture/attribute files, especially `FinancePostgreSqlFixture.cs`, `ChromaFactAttribute.cs`, `ContainerIntegrationFactAttribute.cs`, live Ollama attributes | Reflection-discovered fixtures and opt-in gates | xUnit discovers these through attributes/interfaces rather than ordinary calls. They control Docker sharing and opt-in external dependencies. | `[Collection]`, `[CollectionDefinition]`, custom Fact usage, test runner behavior | Critical | Retain. | Full offline suite and each opt-in gate. |
| Must retain / frontend | `src/cfo-agent-ui/src/**` except candidates above; `favicon.svg`; npm packages other than the candidate above | React app, contracts, tests, styles, toolchain | Import graph is complete, TypeScript build and Oxlint are clean, CSS selectors correspond to rendered structures, favicon is referenced by `index.html`, and direct npm packages map to scripts/config/source. | Imports/exports, class names/selectors, public URLs, package scripts/config, tests/build/lint | High | Retain. No unused component, exported chat type, stylesheet rule, or direct npm package was proven unused. | npm test/build/lint and Playwright container suite. |

## Generated artifacts

| Classification | Path | Symbol or item | Evidence | References searched | Risk | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Generated/build artifact | `**/bin/`, `**/obj/`, `src/cfo-agent-ui/node_modules/`, `src/cfo-agent-ui/dist/` | Build/dependency output | Present locally and ignored; no tracked entry found. | `git ls-files`, `git status --ignored`, ignore files | Low; regeneration costs time/network | May be cleared in a later cleanup when no process is using them. | Restore/build/test afterward. |
| Generated/test artifact | `artifacts/`, `**/TestResults/`, `src/cfo-agent-ui/test-results/`, `playwright-report/`, `coverage/` | Logs, TRX, browser and coverage output | Ignored by repository rules; no tracked generated result was found. | Tracked/ignored status and output-directory patterns | Low, except logs may be needed for diagnostics | Remove only stale local output; preserve logs needed for an active investigation. | Re-run the producing test when needed; `git status`. |
| Generated artifact that must retain | `tools/CfoAgent.FinanceMcpServer/Data/Migrations/*.Designer.cs`, `FinanceDbContextModelSnapshot.cs` | EF-generated source | Generated but intentionally source-controlled for reproducible PostgreSQL schema. | EF migration registration and tests | Critical if deleted | Retain; regenerate only through a deliberate migration change. | Fresh-database migration and seed tests. |

No accidentally tracked `bin`, `obj`, `dist`, `node_modules`, test result, coverage, log, SQLite database, DLL, PDB, executable, cache, or binary-log file was found. The ignored root `.env` is local deployment configuration, not a cleanup artifact, and must not be deleted or committed.

## Historical documentation

| Classification | Path | Symbol or item | Evidence | References searched | Risk | Proposed action | Validation required |
|---|---|---|---|---|---|---|---|
| Historical documentation / must retain | `docs/PHASE-1-RESULTS.md` through `docs/PHASE-8-RESULTS.md`, including Phase 7 design and Phase 8 baseline/migration design | Phase records | Several documents describe superseded SQLite/stdio states; Phase 4, 5, 7, and Phase 8 pre-migration docs are already marked historical. Current docs link to them for traceability. | Root docs links, phase results, architecture guide | Medium if mistaken for active instructions | Retain as historical records. Add a concise historical banner to older unmarked phase results where useful; do not use them as runbooks. | Documentation link and current-state consistency review. |
| Historical documentation / must retain | `tasks/phase-0/**` through `tasks/phase-8/**`, task manifests and execution orders | Completed task specifications | They explain acceptance decisions and are referenced by results/design records. SQLite/stdio text is historical evidence, not active runtime configuration. | Task manifests, execution order, docs links | Medium | Retain under a clearly historical task archive policy. | Link check; confirm current cleanup execution order remains distinct. |

## Explicit legacy and duplication conclusions

- **Old stdio MCP process code:** no active runtime `StdioClientTransport`, `WithStdioServerTransport`, process-launch, server-project-path discovery, or stdio package usage was found. `ProcessStartInfo` remains only in tests that launch independent HTTP hosts or CLI migration commands. The obsolete Phase 5 script still contains `ServerProjectPath` and is listed above.
- **SQLite:** no active SQLite source, package, migration, database file, connection string, or API persistence remains. Only historical docs/tasks, assertions proving SQLite absence, stale manual instructions, and three `.gitignore` patterns remain.
- **Phase 7/8 transitional code:** no runtime transport or persistence transition layer was found. Historical design/result docs are retained. The stale local E2E harness is the main transitional infrastructure candidate.
- **Fallback code:** Finance fallback is absent. Knowledge local fallback is configuration-controlled, Development-only, covered by tests, and must retain.
- **Duplicate DTOs:** MCP server DTOs, private client transport DTOs, and API result DTOs are deliberate process-boundary types and must retain. No removable DTO duplicate was proven.
- **Configuration keys:** all current options properties have binding, runtime, test, Compose, or documented opt-in use. The issue is conflicting default values, not an unused key.
- **Packages:** all direct runtime NuGet packages and direct npm packages have source/config/script usage. `coverlet.collector` is the sole package candidate requiring external-CI proof.
- **Docker:** no dead service, network, volume, health check, or exposed port was found. Integration profiles are intentional.
- **Frontend:** `public/icons.svg` is the only proven unused tracked asset. No unused React component, chat type, import, or CSS selector was proven by reference search, TypeScript build, and Oxlint.
- **Test helpers:** fixtures and custom Fact attributes are reflection-discovered and must retain. One template metadata test is safely duplicate; two broader test groups need an assertion matrix before reduction.

## Recommended cleanup sequence

1. Restore the committed Mock default and re-establish the 176-test offline backend gate before deleting anything.
2. Remove only the safe anchors/assets/sample request/duplicate test, then validate exact expected test-count changes.
3. Decide whether local non-container E2E remains supported; either remove its obsolete harness as a unit or rewrite it for Phase 8.
4. Resolve script and package candidates after checking untracked developer/CI usage.
5. Update stale active documentation and `.gitignore`; preserve historical phase/task records.
6. Run Debug, Release, frontend, Compose, Playwright container, and isolated container gates in the final cleanup task.

## Task acceptance assessment

- `docs/CLEANUP-INVENTORY.md` exists: complete.
- No runtime or test file modified: complete.
- Every proposed deletion includes evidence, risk, action, and validation: complete.
- Reflection, DI, EF Core, MCP discovery, serialization, configuration, Docker, scripts, frontend, and tests considered: complete.
- Repository baseline remains green: **blocked by the pre-existing committed Ollama default**; build and frontend gates pass, but the required backend suite has 7 failures. No attempt was made to fix it because this task permits only the inventory document.

## TASK-CLEANUP-002 outcome

The following proven-safe backend cleanup was applied. The required Debug and Release gates each completed with 175 discovered tests: 167 passed, 0 failed, and 8 opt-in tests skipped. This resolves the baseline blocker recorded above without changing the supported provider architecture.

- Removed `src/CfoAgent.Api/CfoAgent.Api.http`: its only `/weatherforecast/` request is not mapped or referenced.
- Removed `tests/CfoAgent.Api.Tests/UnitTest1.cs`: its Mock metadata assertions are duplicated by `MockChatClientTests.ReturnsAStableUnsupportedResponseAndMockMetadata`.
- Removed redundant `src/.gitkeep`, `tests/.gitkeep`, and `tools/.gitkeep` anchors: each parent directory contains tracked project content and no reference to an anchor was found.
- Restored the documented committed offline default in `src/CfoAgent.Api/appsettings.json` to `AI:Provider=Mock` and `AI:Model=DeterministicMock`. Ollama remains an explicit opt-in provider through the existing configuration path.
- No runtime stdio process-launch code, SQLite package/source/configuration, obsolete Finance fallback, duplicate transport DTO, or unused direct runtime package was found. No package was removed.
- Retained the three SQLite ignore patterns in `.gitignore`: an existing ignored `data/cfo-agent.db` is present locally. Removing the patterns would surface user data, while this task must not delete or relocate application data.
- Retained `data/imports/.gitkeep`, `data/knowledge/.gitkeep`, the empty local frontend scaffold directory, frontend assets, scripts, Docker files, and documentation because they are outside this backend/MCP cleanup scope or require proof beyond this task.

## TASK-CLEANUP-003 outcome

- Removed `src/cfo-agent-ui/public/icons.svg`: repository-wide searches found no import, HTML public URL, CSS URL, Vite, Nginx, test, or Playwright reference. `favicon.svg` remains referenced by `index.html` and was retained.
- Removed the empty, untracked `src/cfo-agent-ui/src/assets/` Vite scaffold directory after confirming it had no entries or references.
- Updated the stale `Mock LLM` Playwright assertion in `e2e/chat.spec.ts` to confirm that the intentionally retired, provider-specific label is absent. The test still exercises the same page and all seven existing scenarios; no E2E case was deleted or skipped.
- Retained every direct npm dependency and devDependency: each has an active application import, TypeScript/Vite/Vitest/Playwright/Oxlint configuration use, or test setup use. No npm package was removed.
- Retained components, TypeScript contracts, CSS, API client behavior, selectors, `favicon.svg`, the container Playwright configuration, and the older local Playwright harness. No other frontend candidate was proven safe; the local harness also depends on a script outside this task's scope.
- `npm ci`, unit tests (2 files, 10 tests), and the production build pass. The stable container Playwright command was run against the existing healthy deployment, but five data-workflow scenarios exceeded the old five-second deterministic-response wait because the user deployment currently selects Ollama. This environment-specific result is not caused by the removed asset or the provider-neutral assertion; no Compose or provider configuration was changed by this task.
